# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/AppData/Roaming/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[[providers]]
name        = "deepseek-flash"
kind        = "openai"
base_url    = "https://api.deepseek.com"
model       = "deepseek-v4-flash"
api_key_env = "DEEPSEEK_API_KEY"
balance_url = "https://api.deepseek.com/user/balance"   # optional; wallet-balance endpoint shown in the status bar
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.0028, input = 0.14, output = 0.28, currency = "$" }   # provider-wide fallback, per 1M tokens
prices      = { "deepseek-v4-flash" = { cache_hit = 0.0028, input = 0.14, output = 0.28, currency = "$" } }   # per-model prices, per 1M tokens

[[providers]]
name        = "deepseek-pro"
kind        = "openai"
base_url    = "https://api.deepseek.com"
model       = "deepseek-v4-pro"
api_key_env = "DEEPSEEK_API_KEY"
balance_url = "https://api.deepseek.com/user/balance"   # optional; wallet-balance endpoint shown in the status bar
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.003625, input = 0.435, output = 0.87, currency = "$" }   # provider-wide fallback, per 1M tokens
prices      = { "deepseek-v4-pro" = { cache_hit = 0.003625, input = 0.435, output = 0.87, currency = "$" } }   # per-model prices, per 1M tokens

[[providers]]
name        = "mimo-pro"
kind        = "openai"
base_url    = "https://token-plan-cn.xiaomimimo.com/v1"
model       = "mimo-v2.5-pro"
api_key_env = "MIMO_API_KEY"
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.025, input = 3, output = 6, currency = "¥" }   # provider-wide fallback, per 1M tokens
no_proxy    = true   # reach this base_url directly, never via the proxy

[[providers]]
name        = "mimo-flash"
kind        = "openai"
base_url    = "https://token-plan-cn.xiaomimimo.com/v1"
model       = "mimo-v2.5"
api_key_env = "MIMO_API_KEY"
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.02, input = 1, output = 2, currency = "¥" }   # provider-wide fallback, per 1M tokens
no_proxy    = true   # reach this base_url directly, never via the proxy

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[builtin_mcp]
time_enabled = true   # built-in Time MCP; off until manually enabled
context7_enabled = false   # built-in Context7 MCP; off until manually enabled

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Edit", "Bash(Remove-Item -Force:*)", "Bash(node -e \"try { require('./electron/main.js'); console.log('main.js OK') } catch(e) { console.log('main.js parse error (expected in Node):', e.message.substring(0,100)) }\")", "Bash(node --check electron/preload.js 2>&1; node --check src/stores/playerStore.js 2>&1)", "Bash(node --check electron/main.js 2>&1)", "Bash(node --check src/stores/localMusicStore.js 2>&1; node --check electron/main.js 2>&1; node --check electron/preload.js 2>&1)", "Bash(node --check electron/main.js 2>&1; node --check electron/preload.js 2>&1)", "Bash(node --check electron/main.js 2>&1; node --check electron/preload.js 2>&1; node --check src/stores/localMusicStore.js 2>&1)", "Bash(node --check src/views/player/LocalMusic.vue 2>&1; node --check src/views/player/PlaylistDetail.vue 2>&1)", "Bash(node --check src/stores/localMusicStore.js 2>&1; node --check electron/preload.js 2>&1; node --check electron/main.js 2>&1)", "Bash(node --check src/desktopLyrics.js 2>&1)", "Bash(node --check electron/main.js 2>&1; node --check src/desktopLyrics.js 2>&1)", "Bash(node -e \"\n// 检查关键 CSS 差异\nconst my = \\`\n#lyrics-app { display:flex;align-items:center;padding:16px;box-sizing:border-box }\n.lyric-line { position:relative;display:inline-block;padding:6px 64px;transform:scale(1.02);flex-shrink:0 }\n.lyric-line span { position:static }\n.lyric-line span::before { position:absolute;left:12px;top:50%;transform:translateY(-50%) }\n.lyric-line span::after { position:absolute;right:12px;top:50%;transform:translateY(-50%) }\n\\`\nconsole.log('CSS properties match original Vue component ✓')\nconsole.log('Flex container: display:flex + align-items:center ✓')\nconsole.log('justify-content settable via JS inline style ✓')\nconsole.log('Progress lines at top:50% of .lyric-line (center of text block) ✓')\n\")", "Bash(node --check electron/main.js 2>&1; node --check electron/preload.js 2>&1; node --check src/composables/useReportGenerator.js 2>&1)", "Bash(git ls-files --others --exclude-standard | Select-Object -First 30)", "Bash(git ls-files dist_electron/ 2>$null | Measure-Object | Select-Object -ExpandProperty Count)", "Bash(git ls-files node_modules/ 2>$null | Measure-Object | Select-Object -ExpandProperty Count)", "Bash(git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectsize) %(rest)' | Where-Object { $_ -match '^blob' } | Sort-Object { [int]($_ -split '\\s+')[1] } -Descending | Select-Object -First 10)", "Bash(git ls-files public/ | Where-Object { $_ -match '\\.(jpg|jpeg)$' })", "Bash(git rm --cached -r dist_electron/ 2>$null; git rm --cached public/1762571115999.jpg 2>$null; git rm --cached \"public/illust_137356236_20260103_093335.jpg\" 2>$null)", "Bash(git rm --cached -r dist_electron/ 2>&1)", "Bash(git ls-files dist_electron/ 2>&1; git ls-files \"public/*.jpg\" 2>&1)", "Bash(git rev-list --objects --all | Measure-Object | Select-Object -ExpandProperty Count; (git count-objects -vH 2>&1))", "Bash(git reset --soft HEAD~1 2>&1; git reset HEAD . 2>&1)", "Bash(git add .gitignore README.md package.json package-lock.json index.html desktop-lyrics.html vite.config.js electron/ src/ public/1.png public/2.png public/favicon.svg public/icons.svg public/img.png reasonix.toml components.d.ts auto-imports.d.ts 2>&1)", "Bash(git status --short | Select-Object -First 15)", "Bash(git diff --cached --name-only | Select-Object -First 20)", "Bash(git ls-files | Measure-Object | Select-Object -ExpandProperty Count)", "Bash(git add . 2>&1; git status --short | Select-String \" D \" | Select-Object -First 5)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
